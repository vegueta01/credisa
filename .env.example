# Copia este archivo a ".env" (ese sí NO se sube a git) y pon un valor propio.
# Es la contraseña/token que protege el panel de administración (/panel-XXXXXXXXXX/).
# Genera uno nuevo con:  openssl rand -hex 24
ADMIN_TOKEN=cambia-esto-por-un-token-largo-y-aleatorio
